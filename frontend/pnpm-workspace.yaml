# This file is used to configure the pnpm workspace for the frontend project.
# It specifies which packages are included in the workspace and which ones are allowed to be built.
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  lmdb: true
  msgpackr-extract: true
