/**
 * @see https://prettier.io/docs/configuration
 * @type {{tabWidth: number, useTabs: boolean, singleQuote: boolean, semi: boolean, racketSpacing: boolean, arrowParens: string, trailingComma: string, bracketSameLine: boolean, printWidth: number}}
 */
const config = {
  tabWidth: 2,
  useTabs: false,
  singleQuote: true,
  semi: true,
  racketSpacing: true,
  arrowParens: 'avoid',
  trailingComma: 'es5',
  bracketSameLine: true,
  printWidth: 80,
  endOfLine: 'crlf',
};

export default config;
