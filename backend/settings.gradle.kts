/**
 * Das ist die Einstellungsdatei für das Backend-Projekt.
 * Hier wird der Name des Projekts festgelegt, damit es in der Build-Umgebung korrekt identifiziert werden kann.
 */
rootProject.name = "backend"
